version: '1.0'
services:
  nginx:
    image: nginx:latest
    container_name: ai2god
    restart: unless-stopped
    ports:
      - "80:881"
      - "443:4443"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
      - ./nssl:/etc/nginx/ssl:ro
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
